# Codecov configuration
# https://docs.codecov.com/docs/codecovyml-reference

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto

ignore:
  # Entry points, integration tested
  - "cmd/**/*"
  # Thin wrapper around stdlib
  - "pkg/logger/**/*"
  # Test files themselves
  - "**/*_test.go"
  # Assembly stubs — not instrumentable by Go's coverage tool
  - "internal/vector/asm"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
